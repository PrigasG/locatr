% Generated by roxygen2: do not edit by hand
% Please edit documentation in R/cache.R
\name{locatr_cache}
\alias{locatr_cache}
\alias{print.locatr_cache}
\title{Create a locatr response cache}
\usage{
locatr_cache(path = NULL, format = c("rds", "parquet"), store_raw = FALSE)

\method{print}{locatr_cache}(x, ...)
}
\arguments{
\item{path}{Optional file path for a persistent cache. \code{NULL} (default) keeps
the cache in memory for the session only - no disk writes. When a path is
given, the cache is loaded from it if present and flushed on every write.}

\item{format}{On-disk format when \code{path} is set: \code{"rds"} (default, no extra
dependency) or \code{"parquet"} (needs \code{arrow}).}

\item{store_raw}{Reserved for storing raw service responses. \code{FALSE} by
default; raw storage is only ever kept for services whose terms permit it.}

\item{x}{A \code{locatr_cache} object.}

\item{...}{Ignored.}
}
\value{
A \code{locatr_cache} object (an environment) to pass to geocoding
functions.
}
\description{
A cache of parsed geocoder results that makes runs reproducible: repeated
queries are served locally instead of re-hitting the service, and - because
the parsed coordinates are stored - a cached result can be replayed offline,
even without the \code{httr}/\code{jsonlite} packages that the live call needs. Pass the
returned object to \code{\link[=geocode_address]{geocode_address()}} via its \code{cache} argument.
}
\details{
The cache table is \emph{long}: one row per candidate result (so
\code{\link[=geocode_address]{geocode_address()}}'s ranked set round-trips exactly), plus a single
sentinel row (\code{result_rank = 0}, \code{status = "no_match"}) for a query that
matched nothing, so misses are replayable and never silently re-queried.

The lookup \code{key} is an implementation detail (a hash). The visible
\code{method}, \code{endpoint}, \code{query}, and \code{params} columns are the audit contract -
keys can always be rebuilt from them if a future \code{rlang} changes its hash.
}
\examples{
cache <- locatr_cache()
cache_info(cache)
}
\seealso{
\code{\link[=cache_info]{cache_info()}}, \code{\link[=cache_clear]{cache_clear()}}, \code{\link[=geocode_address]{geocode_address()}}
}
