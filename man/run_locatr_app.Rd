% Generated by roxygen2: do not edit by hand
% Please edit documentation in R/run-app.R
\name{run_locatr_app}
\alias{run_locatr_app}
\title{Launch the locatr demo Shiny app}
\usage{
run_locatr_app(...)
}
\arguments{
\item{...}{Passed to \code{\link[shiny:runApp]{shiny::runApp()}} (e.g. \code{port}, \code{host}, \code{launch.browser}).}
}
\value{
Called for its side effect of starting the app; does not return.
}
\description{
Runs the bundled web app (the same one published as a Hugging Face Space):
upload a CSV/Excel/Parquet file, geocode it with the locatr cascade, download
the geocoded records directly, or optionally attach local geography from
Census TIGER/Line/an uploaded shapefile before downloading a crosswalk. The
download step can remove selected columns before export. The app is for
demonstration and light interactive use; production pipelines should call the
package functions directly.
}
\details{
The app depends on packages that are only listed under \code{Suggests}, so they
are not installed automatically. If any are missing, this function stops with
the install command you need.
}
\examples{
if (interactive()) {
run_locatr_app()
}
}
