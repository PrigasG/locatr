% Generated by roxygen2: do not edit by hand
% Please edit documentation in R/build-review-overrides.R
\name{build_review_overrides}
\alias{build_review_overrides}
\title{Build a manual-override table from review decisions}
\usage{
build_review_overrides(data, decisions)
}
\arguments{
\item{data}{A geocoded data frame with at least \code{record_id}, and ideally
\code{latitude}/\code{longitude}/\code{record_name}/\code{full_address_clean} for context.}

\item{decisions}{A data frame with \code{record_id}, \code{decision} (one of
\code{"accept"}, \code{"reject"}, \code{"relocate"}), and - for relocations -
\code{manual_latitude} / \code{manual_longitude}.}
}
\value{
A tibble with \code{record_id}, \code{record_name}, \code{full_address_clean},
the current \code{latitude}/\code{longitude}, the reviewer's \code{manual_latitude} /
\code{manual_longitude}, and \code{manual_note}. Compatible with
\code{\link[=apply_manual_overrides]{apply_manual_overrides()}}.
}
\description{
Turns a set of per-record review decisions (accept / reject / relocate) into
a completed override table with the same layout \code{\link[=write_geocode_review]{write_geocode_review()}}
produces, so it can be written to CSV and fed straight to
\code{\link[=apply_manual_overrides]{apply_manual_overrides()}}. This is the testable core behind the map-based
review app (\code{\link[=run_locatr_review_app]{run_locatr_review_app()}}); it holds no reactive or UI code, so
it can also be used headless.
}
\details{
Decision semantics:
\itemize{
\item \code{accept} - confirm the automated coordinate: \code{manual_latitude} /
\code{manual_longitude} are set to the record's current coordinate.
\item \code{relocate} - use the reviewer's coordinate: \code{manual_latitude} /
\code{manual_longitude} come from the \code{decisions} table.
\item \code{reject} - drop the coordinate: \verb{manual_*} are left \code{NA} (so
\code{\link[=apply_manual_overrides]{apply_manual_overrides()}} does not place it) and the note records the
rejection.
}
}
\examples{
geocoded <- data.frame(
  record_id = c("a", "b", "c"),
  record_name = c("A", "B", "C"),
  full_address_clean = c("1 A St", "2 B St", "3 C St"),
  latitude = c(40.1, 40.2, NA),
  longitude = c(-74.1, -74.2, NA)
)
decisions <- data.frame(
  record_id = c("a", "b", "c"),
  decision = c("accept", "relocate", "reject"),
  manual_latitude = c(NA, 40.25, NA),
  manual_longitude = c(NA, -74.25, NA)
)
build_review_overrides(geocoded, decisions)
}
\seealso{
\code{\link[=run_locatr_review_app]{run_locatr_review_app()}}, \code{\link[=apply_manual_overrides]{apply_manual_overrides()}},
\code{\link[=write_geocode_review]{write_geocode_review()}}
}
