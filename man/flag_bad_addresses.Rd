% Generated by roxygen2: do not edit by hand
% Please edit documentation in R/flag-address.R
\name{flag_bad_addresses}
\alias{flag_bad_addresses}
\title{Flag addresses that should not be blindly geocoded}
\usage{
flag_bad_addresses(data)
}
\arguments{
\item{data}{A data frame from \code{\link[=clean_addresses]{clean_addresses()}}.}
}
\value{
\code{data} with added columns \code{bad_address_flag} and \code{review_status}.
Rows fit for geocoding get \code{review_status == "ready_for_geocoding"}.
}
\description{
Identifies PO boxes, placeholders, and missing fields so they go straight to
review instead of wasting geocoder calls (or producing confident-but-wrong
matches). Sets \code{bad_address_flag} and an initial \code{review_status}.
}
\examples{
df <- tibble::tibble(
  record_id = c("a", "b"),
  address_clean = c("100 MAIN STREET", "PO BOX 42"),
  city_clean = c("TRENTON", "TRENTON"),
  zip_clean = c("08608", "08608"),
  record_name = c("Real Site", "Mailbox Co")
)
flag_bad_addresses(df)
}
