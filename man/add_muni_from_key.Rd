% Generated by roxygen2: do not edit by hand
% Please edit documentation in R/add-muni-from-key.R
\name{add_muni_from_key}
\alias{add_muni_from_key}
\title{Add county and municipality fields by joining on a shared key}
\usage{
add_muni_from_key(
  data,
  muni_shapes,
  data_key,
  shp_key,
  county_col = NULL,
  muni_col = NULL,
  key_col = NULL
)
}
\arguments{
\item{data}{A data frame of geocoded records carrying \code{data_key}.}

\item{muni_shapes}{An \code{sf} polygon (or attribute) layer carrying \code{shp_key}
and the geography attributes to attach.}

\item{data_key}{Name (string) of the join-key column in \code{data}.}

\item{shp_key}{Name (string) of the join-key column in \code{muni_shapes}.}

\item{county_col, muni_col}{Optional explicit county / locality column names
in \code{muni_shapes}. Empty strings are treated as unset.}

\item{key_col}{Optional municipal key column in \code{muni_shapes}.}
}
\value{
\code{data} with \code{County}, \code{Municipality}, \verb{Muni Key}, \code{muni_join_key},
\code{county_code}, \code{county_fips}, \code{municipality_code}, \code{municipality_geoid},
\code{municipality_name_standard}, \code{municipality_type}, \code{muni_match_status}, and
the generic \code{location_county} / \code{location_locality} /
\code{geography_match_status} audit fields.
}
\description{
The non-spatial counterpart to \code{\link[=add_muni_from_shapes]{add_muni_from_shapes()}}. Instead of a
point-in-polygon join, it merges geography attributes from a boundary layer
onto geocoded records by a shared key column (for example a ZIP, FIPS, GEOID,
or local region code that both tables carry). Use it when a spatial join is
not the right criterion - the records may lack coordinates, or the
authoritative geography is keyed by a code rather than a polygon footprint.
}
\details{
Output columns match \code{\link[=add_muni_from_shapes]{add_muni_from_shapes()}} exactly, so the two join paths
are interchangeable downstream: \code{County}, \code{Municipality}, \verb{Muni Key},
\code{muni_join_key}, the stable code/identifier fields, \code{location_county},
\code{location_locality}, \code{geography_match_status}, and \code{muni_match_status}.
Stable code fields (\code{county_code}, \code{municipality_code}, \code{municipality_geoid},
etc.) are auto-detected from common boundary schemas; \code{county_fips} is
synthesised from a state FIPS plus county code when not supplied directly,
and \verb{Muni Key} falls back to \code{County::Municipality} when no official
identifier exists.
}
\seealso{
\code{\link[=add_muni_from_shapes]{add_muni_from_shapes()}} for the spatial (point-in-polygon) join.
}
