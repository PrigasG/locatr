% Generated by roxygen2: do not edit by hand
% Please edit documentation in R/build-local-geography.R
\name{build_local_geography}
\alias{build_local_geography}
\title{Build a local geography layer from Census TIGER/Line boundaries}
\usage{
build_local_geography(
  state,
  geography = c("county_subdivision", "place", "county", "tract"),
  county = NULL,
  year = NULL,
  cb = TRUE,
  ...
)
}
\arguments{
\item{state}{Two-letter state abbreviation or FIPS code (passed to
\pkg{tigris}).}

\item{geography}{Which Census layer becomes \code{location_locality}. One of
\code{"county_subdivision"}, \code{"place"}, \code{"county"}, \code{"tract"}.}

\item{county}{Optional county filter (name or FIPS) for
\code{"county_subdivision"}/\code{"tract"}; passed to \pkg{tigris}.}

\item{year}{Vintage year for the boundary files. \code{NULL} uses the
\pkg{tigris} default.}

\item{cb}{If \code{TRUE} (default), use the smaller cartographic boundary files;
\code{FALSE} pulls the full-resolution TIGER/Line files.}

\item{...}{Passed through to the underlying \pkg{tigris} download function.}
}
\value{
An \code{sf} polygon layer in WGS84 (EPSG:4326) with
\code{location_county}, \code{location_locality}, stable join-code columns when
available, and \code{geometry}, ready for
\code{add_local_geography(geography_shapes = ...)}.
}
\description{
Downloads an authoritative Census boundary layer for a state with the
\pkg{tigris} package and standardises it into the two-column schema
\code{\link[=add_local_geography]{add_local_geography()}} expects: \code{location_county} (always from counties) and
\code{location_locality} (from the requested \code{geography}). It also carries stable
Census identifiers such as \code{county_fips}, \code{municipality_geoid}, and
\code{muni_join_key} when those fields exist. This makes "locality" a configurable
concept, because what counts as a municipality is not consistent across
states.
}
\details{
Choosing \code{geography}:
\itemize{
\item \code{"county_subdivision"} (default) maps well to townships/municipalities in
states like NJ, PA, NY and New England. Best general default for "locality".
\item \code{"place"} maps to incorporated places and CDPs, but misses townships and
many unincorporated areas. Places can also straddle counties, so each place
is assigned the county it overlaps most.
\item \code{"county"} sets locality to the county itself.
\item \code{"tract"} uses the census tract identifier as the locality (analysis, not
administrative naming).
}

Census TIGER/Line is the best scalable baseline nationwide. For high-stakes,
state-specific reporting where "municipality" has legal meaning, prefer an
official state GIS layer and pass it straight to \code{\link[=add_local_geography]{add_local_geography()}} -
that path works regardless, since the join accepts any polygon layer.
}
\examples{
if (interactive()) {
areas <- build_local_geography(state = "PA", geography = "county_subdivision")
final <- add_local_geography(geocoded, geography_shapes = areas)
}
}
