% Generated by roxygen2: do not edit by hand
% Please edit documentation in R/geocode-fallback.R
\name{geocode_fallback}
\alias{geocode_fallback}
\title{Fallback geocode pass via ArcGIS (Google-like fuzzy matching)}
\usage{
geocode_fallback(data, method = "arcgis", bbox = region_bbox("NJ"), ...)
}
\arguments{
\item{data}{A data frame from \code{\link[=geocode_census]{geocode_census()}} (or after
\code{\link[=validate_geocodes]{validate_geocodes()}}).}

\item{method}{tidygeocoder method for the fallback (default \code{"arcgis"}).
\code{"google"} also works if \code{GOOGLEGEOCODE_API_KEY} is set.}

\item{bbox}{Bounding box used to reject out-of-region matches; see
\code{\link[=region_bbox]{region_bbox()}}.}

\item{...}{Passed through to \code{\link[tidygeocoder:geocode]{tidygeocoder::geocode()}}.}
}
\value{
\code{data} with fallback columns \code{fb_latitude}, \code{fb_longitude},
\code{fb_status}, and updated \code{latitude}, \code{longitude}, \code{geocode_method},
\code{geocode_pass}, \code{match_status} for rows the fallback filled.
}
\description{
For rows the Census pass could not place inside the configured region,
re-geocodes with
a composite geocoder (default ArcGIS: free, no API key, fuzzy matching close
to Google) using the single-line \code{full_address_clean}. ArcGIS requests are
constrained to the region bbox when possible, and results are still guarded
against the bounding box so out-of-region false matches are discarded before
coordinates are coalesced back into \code{latitude}/\code{longitude}.
}
