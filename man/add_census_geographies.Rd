% Generated by roxygen2: do not edit by hand
% Please edit documentation in R/add-census-geographies.R
\name{add_census_geographies}
\alias{add_census_geographies}
\title{Attach multiple Census geography levels to geocoded points}
\usage{
add_census_geographies(
  data,
  state,
  levels = "tract",
  county = NULL,
  year = NULL,
  cb = TRUE,
  ...
)
}
\arguments{
\item{data}{A geocoded data frame with \code{latitude} and \code{longitude} columns.}

\item{state}{Two-letter state abbreviation or FIPS code (passed to
\pkg{tigris}). ZCTAs are national, so \code{state} is ignored for the \code{"zcta"}
level.}

\item{levels}{Character vector of geography levels to attach. Any of
\code{"tract"}, \code{"block_group"}, \code{"zcta"}, \code{"county"}, \code{"place"},
\code{"county_subdivision"}, \code{"congressional_district"},
\code{"state_legislative_district_upper"},
\code{"state_legislative_district_lower"}, \code{"school_district"} (unified).
Defaults to \code{"tract"}.}

\item{county}{Optional county filter (name or FIPS) for the levels that
accept one (\code{"tract"}, \code{"block_group"}, \code{"county_subdivision"}).}

\item{year}{Vintage year for the boundary files. \code{NULL} uses the \pkg{tigris}
default.}

\item{cb}{If \code{TRUE} (default), use the smaller cartographic boundary files.}

\item{...}{Passed through to the underlying \pkg{tigris} download functions.}
}
\value{
\code{data} with, for each requested level, a \verb{<level>_geoid} and
\verb{<level>_name} column. Rows without usable coordinates get \code{NA}.
}
\description{
Enriches geocoded records with one or more Census TIGER/Line geography levels
in a single call, by point-in-polygon assignment. Where \code{\link[=add_county_muni]{add_county_muni()}}
answers "which county and municipality", this answers "which tract, block
group, ZCTA, congressional district, state legislative district, or school
district" - the analysis and policy geographies that dashboards and grant
reporting often need. Each requested level adds two columns:
\verb{<level>_geoid} (the Census GEOID) and \verb{<level>_name} (the layer's name field,
\code{NA} where the layer has none, e.g. ZCTAs).
}
\details{
The geography step only needs \code{latitude}/\code{longitude}; it does not touch the
address columns. Downloads use \pkg{tigris} and therefore need network access.
}
\examples{
if (interactive()) {
  enriched <- add_census_geographies(
    geocoded, state = "NJ",
    levels = c("tract", "congressional_district", "school_district")
  )
}
}
\seealso{
\code{\link[=add_county_muni]{add_county_muni()}} for county/municipality, \code{\link[=build_local_geography]{build_local_geography()}}
for a single reusable boundary layer.
}
