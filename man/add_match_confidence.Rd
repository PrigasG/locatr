% Generated by roxygen2: do not edit by hand
% Please edit documentation in R/match-confidence.R
\name{add_match_confidence}
\alias{add_match_confidence}
\title{Add a unified match-confidence score}
\usage{
add_match_confidence(data)
}
\arguments{
\item{data}{A data frame from the batch pipeline or from \code{\link[=geocode_address]{geocode_address()}}.}
}
\value{
\code{data} with two added columns: \code{match_confidence} (0-1, rounded to
three decimals) and \code{confidence_reason}.
}
\description{
Collapses locatr's several trust signals into one calibrated
\code{match_confidence} on a 0-1 scale plus a short \code{confidence_reason} string, so
a reviewer can sort or threshold on a single column instead of reading
\code{match_status}, \code{validation_status}, \code{nm_status}, and \code{review_status}
together. Higher is more trustworthy.
}
\details{
The right scoring model is chosen from the columns present:
\itemize{
\item Pipeline output (from \code{\link[=geocode_records]{geocode_records()}} / the crosswalk): scored
from the tier that placed the row (\code{geocode_pass}), the match and
validation status, and the name-tier confidence. Rejected or unplaced
rows score near zero; reference-verified and manual rows score highest.
\item Candidate output (from \code{\link[=geocode_address]{geocode_address()}}): scored from the ArcGIS
match score, discounted by how coarse the address type is, and capped
when the point falls outside a supplied \code{bbox}.
}

The score is a transparent, rule-based prior - deliberately explainable
rather than a black-box model - so every value can be traced to its
\code{confidence_reason}.
}
\examples{
add_match_confidence(data.frame(
  geocode_pass = c("pass_1_census_structured", "pass_4_name_lookup"),
  match_status = c("matched", "matched_low_confidence"),
  validation_status = c("coordinate_ok", "coordinate_ok"),
  latitude = c(40.2, 40.3), longitude = c(-74.7, -74.8)
))
}
