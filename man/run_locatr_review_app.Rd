% Generated by roxygen2: do not edit by hand
% Please edit documentation in R/run-review-app.R
\name{run_locatr_review_app}
\alias{run_locatr_review_app}
\title{Launch the locatr map-based review app}
\usage{
run_locatr_review_app(...)
}
\arguments{
\item{...}{Passed to \code{\link[shiny:runApp]{shiny::runApp()}} (e.g. \code{port}, \code{host},
\code{launch.browser}).}
}
\value{
Called for its side effect of starting the app; does not return.
}
\description{
A standalone Shiny app that takes records from upload through geocoding to
map-based review. Upload a file, map the ID and address columns, then either
geocode the addresses (clean, flag, cascade, and attach county/municipality)
or use existing coordinates. The flagged records appear on a map to accept,
reject, or relocate; the app builds a completed override table with
\code{\link[=build_review_overrides]{build_review_overrides()}} that you download as CSV and feed to
\code{\link[=apply_manual_overrides]{apply_manual_overrides()}}. Geocoding calls external services and needs
network access.
}
\details{
The app depends on packages listed only under \code{Suggests}, so they are not
installed automatically. If any are missing, this function stops with the
install command you need.
}
\examples{
\dontrun{
run_locatr_review_app()
}
}
\seealso{
\code{\link[=build_review_overrides]{build_review_overrides()}}, \code{\link[=apply_manual_overrides]{apply_manual_overrides()}}
}
