% Generated by roxygen2: do not edit by hand
% Please edit documentation in R/geocode-census.R
\name{geocode_census}
\alias{geocode_census}
\title{Primary geocode pass via the US Census batch geocoder}
\usage{
geocode_census(data, ...)
}
\arguments{
\item{data}{A data frame from \code{\link[=flag_bad_addresses]{flag_bad_addresses()}}.}

\item{...}{Passed through to \code{\link[tidygeocoder:geocode]{tidygeocoder::geocode()}} (e.g. \code{full_results}).}
}
\value{
\code{data} with \code{latitude}, \code{longitude}, \code{geocode_method},
\code{geocode_pass}, \code{match_status}, plus Census full-result columns when
\code{full_results = TRUE}.
}
\description{
Geocodes only the rows marked \code{ready_for_geocoding}, using the \emph{structured}
Census engine (street / city / state / ZIP) rather than a single-line string,
which matches reliably more often. Rows not ready are returned untouched with
empty coordinate columns so the frame stays rectangular.
}
\details{
Volatile Census full-result columns (\code{tiger_line_id}, \code{id}) are coerced to
character to avoid the \code{bind_rows()} integer/character type clash that the
Census service triggers intermittently between batches.
}
