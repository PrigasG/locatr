% Generated by roxygen2: do not edit by hand
% Please edit documentation in R/geocode-address.R
\name{geocode_address}
\alias{geocode_address}
\title{Look up a single address and return ranked candidate points}
\usage{
geocode_address(
  address,
  city = NULL,
  state = NULL,
  zip = NULL,
  id = NULL,
  min_score = 0,
  max_candidates = 5L,
  geography = TRUE,
  geography_shapes = NULL,
  bbox = NULL,
  quiet = TRUE,
  show_progress = interactive()
)
}
\arguments{
\item{address}{Single-line street address as a length-1 character string.}

\item{city}{Optional locality for the address (length-1 character).}

\item{state}{Optional two-letter state abbreviation. When \code{city} is supplied
but \code{state} is omitted, defaults to \code{"NJ"} for compatibility.}

\item{zip}{Optional ZIP/postal code. Improves match precision when supplied.}

\item{id}{Optional label echoed back in the \code{query_id} column.}

\item{min_score}{Minimum ArcGIS match score (0-100) a candidate must reach to
be returned. Defaults to \code{0} (return all, still ranked).}

\item{max_candidates}{Maximum number of candidates to return. Defaults to \code{5}.}

\item{geography}{If \code{TRUE} (default), attach \code{County}/\code{Municipality} (and the
other local-geography fields). When \code{state} is not supplied, locatr tries
to infer candidate states from ArcGIS matched addresses. Set \code{FALSE} for
coordinates only.}

\item{geography_shapes}{Optional \code{sf} boundary layer to attach geography from
(via \code{\link[=add_muni_from_shapes]{add_muni_from_shapes()}}). When \code{NULL} and \code{geography = TRUE}, county
subdivisions are built from Census TIGER/Line for \code{state} (needs \code{tigris}
and network access).}

\item{bbox}{Optional region bounding box (see \code{\link[=region_bbox]{region_bbox()}}). When given,
ArcGIS is asked to prefer that extent and an \code{in_bbox} flag is added.}

\item{quiet}{If \code{TRUE} (default), suppress routine messages from geography
downloads/joins so the console shows only the returned candidate table.}

\item{show_progress}{If \code{TRUE}, print short progress messages while the
lookup runs. Defaults to \code{interactive()}.}
}
\value{
A tibble of candidates ordered by descending \code{match_score}, with
\code{query_id}, \code{rank}, \code{match_score}, \code{match_addr_type}, \code{matched_address},
\code{latitude}, \code{longitude}, the cleaned \code{input_address}, an optional \code{in_bbox}
flag, and (when \code{geography = TRUE}) \code{County}/\code{Municipality} and related
fields. Zero rows if nothing matched at or above \code{min_score}.
}
\description{
An interactive, one-shot companion to the batch pipeline: pass a literal
address (no data frame) and get back a tibble of candidate matches ranked by
geocoder confidence, highest first. Each candidate carries its coordinates,
match score, and ArcGIS address type, and - unless turned off - the county
and municipality the point falls in. Handy for spot-checking one address, or
for letting a reviewer eyeball the plausible locations the cascade would
choose from.
}
\details{
The address text is normalised with the same abbreviation/secondary-unit
cleaning used by \code{\link[=clean_addresses]{clean_addresses()}}, then sent to the free ArcGIS
\code{findAddressCandidates} service. \code{city}, \code{state}, and \code{zip} are optional for
this one-off helper: use them when you want to narrow the search, or pass only
\code{address} to inspect broad candidate matches. If \code{city} is supplied and
\code{state} is omitted, \code{state} defaults to \code{"NJ"} for compatibility with the
package's first workflow.
}
\examples{
if (interactive()) {
# ranked candidates for one address
geocode_address("1600 Pennsylvania Ave NW")

# only high-confidence matches, coordinates only
geocode_address("1 City Hall Sq", city = "Boston", state = "MA",
                min_score = 90, geography = FALSE)
}
}
\seealso{
\code{\link[=geocode_records]{geocode_records()}} for the batch cascade over a data frame.
}
