% Generated by roxygen2: do not edit by hand
% Please edit documentation in R/flag-field-conflicts.R
\name{flag_field_conflicts}
\alias{flag_field_conflicts}
\title{Flag cross-field conflicts in location data}
\usage{
flag_field_conflicts(
  data,
  zip = "zip_clean",
  state = "state_clean",
  stated_county = NULL,
  geocoded_county = "location_county"
)
}
\arguments{
\item{data}{A data frame of cleaned/geocoded records.}

\item{zip}{Name of the ZIP column (default \code{"zip_clean"}). Set to \code{NULL} to
skip the ZIP check.}

\item{state}{Name of the state column (default \code{"state_clean"}).}

\item{stated_county}{Optional name of a county column supplied in the input.
The county check runs only when this is given.}

\item{geocoded_county}{Name of the geocoded county column to compare against
(default \code{"location_county"}).}
}
\value{
\code{data} with three added columns: \code{zip_state_conflict} (logical, \code{NA}
when indeterminate), \code{county_conflict} (logical, \code{NA} when either county is
missing), and \code{field_conflict} (a \code{"; "}-joined summary such as
\code{"zip_state"}, \code{"county"}, or \code{"zip_state; county"}; \code{NA} when clean).
}
\description{
Catches a class of data-entry errors the geocoder itself will silently accept:
a ZIP that cannot belong to the stated state, and a stated county that
disagrees with the county the coordinate actually fell in. It adds audit
columns rather than changing any coordinate, so a reviewer can decide what to
do.
}
\details{
The ZIP check is deliberately conservative. It compares the ZIP's leading
digit against the USPS regional assignment for the stated state, so it only
flags a ZIP that is definitively in the wrong region (for example a
\code{"8xxxx"} ZIP recorded in New Jersey). It never flags a same-region
near-miss, and it stays silent when the state is unknown or the ZIP is
missing, so it does not produce false positives.

The county check compares a stated county column against a geocoded county
column (for example \code{location_county} from \code{\link[=add_county_muni]{add_county_muni()}}), after
normalising case and stripping the trailing "County"/"Parish"/"Borough".
}
\examples{
df <- data.frame(
  zip_clean = c("07030", "85001"),   # 07 is NJ; 85 is AZ
  state_clean = c("NJ", "NJ")
)
flag_field_conflicts(df)
}
