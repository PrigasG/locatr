% Generated by roxygen2: do not edit by hand
% Please edit documentation in R/locatr-package.R
\name{bbox_from_sf}
\alias{bbox_from_sf}
\title{Build a geocoding bounding box from an sf layer}
\usage{
bbox_from_sf(geography_shapes, buffer = 0.05)
}
\arguments{
\item{geography_shapes}{An \code{sf} object.}

\item{buffer}{Numeric buffer in decimal degrees added to each side of the
bounding box. Defaults to \code{0.05} to avoid rejecting edge locations.}
}
\value{
A named numeric vector with \code{lat_min}, \code{lat_max}, \code{lon_min}, and
\code{lon_max}.
}
\description{
Converts any point, line, or polygon \code{sf} layer to WGS84 and returns a named
latitude/longitude bounding box suitable for \code{\link[=geocode_records]{geocode_records()}},
\code{\link[=geocode_arcgis]{geocode_arcgis()}}, \code{\link[=geocode_by_name]{geocode_by_name()}}, and \code{\link[=validate_geocodes]{validate_geocodes()}}. This is
the safest way to keep multi-state geocoding and local geography joins aligned:
build or load the geography layer first, then derive the bbox from it.
}
\examples{
\dontrun{
areas <- build_local_geography("PA")
bbox <- bbox_from_sf(areas)
}
}
