% Generated by roxygen2: do not edit by hand
% Please edit documentation in R/provenance.R
\name{geocode_provenance}
\alias{geocode_provenance}
\alias{print.locatr_provenance}
\title{Read the provenance manifest from a geocoding run}
\usage{
geocode_provenance(data)

\method{print}{locatr_provenance}(x, ...)
}
\arguments{
\item{data}{Output of \code{\link[=geocode_records]{geocode_records()}}.}

\item{x}{A \code{locatr_provenance} object.}

\item{...}{Ignored.}
}
\value{
A \code{locatr_provenance} object (a named list) describing the run.
}
\description{
\code{\link[=geocode_records]{geocode_records()}} attaches a run manifest as an attribute of its output.
This returns it: a run id and UTC timestamp, the locatr / tidygeocoder /
cache-schema versions, the tiers run, whether a reference table was used, the
cache path, per-\code{review_status} counts, and cache activity
(\code{cache_hits} / \code{cache_misses} / \code{cache_writes}). Read it directly on the
\code{geocode_records()} result, since later data-frame operations may drop the
attribute.
}
\seealso{
\code{\link[=geocode_records]{geocode_records()}}, \code{\link[=locatr_cache]{locatr_cache()}}
}
