% Generated by roxygen2: do not edit by hand
% Please edit documentation in R/add-local-geography.R
\name{add_county_muni}
\alias{add_county_muni}
\title{Add county and municipality fields from Census boundaries}
\usage{
add_county_muni(
  data,
  state,
  geography = c("county_subdivision", "place", "county", "tract"),
  county = NULL,
  year = NULL,
  cb = TRUE,
  ...
)
}
\arguments{
\item{data}{A validated data frame with \code{latitude}/\code{longitude}.}

\item{state}{Two-letter state abbreviation or FIPS code.}

\item{geography}{Which Census layer should become \code{Municipality}; passed to
\code{\link[=build_local_geography]{build_local_geography()}}.}

\item{county}{Optional county filter for supported Census layers.}

\item{year}{Vintage year for the boundary files.}

\item{cb}{If \code{TRUE}, use smaller cartographic boundary files.}

\item{...}{Passed through to \code{\link[=build_local_geography]{build_local_geography()}}.}
}
\value{
\code{data} with \code{County}, \code{Municipality}, \verb{Muni Key}, and
\code{muni_match_status}, plus the generic \verb{location_*} geography audit fields.
}
\description{
Convenience wrapper for the common workflow where users want county and
municipality/locality fields but do not already have a boundary file. It
builds a Census TIGER/Line geography layer with \code{\link[=build_local_geography]{build_local_geography()}} and
then applies \code{\link[=add_muni_from_shapes]{add_muni_from_shapes()}}.
}
\details{
For reporting where municipality has a state-specific legal definition, use
\code{\link[=add_muni_from_shapes]{add_muni_from_shapes()}} with an official state/local GIS layer.
}
