% Generated by roxygen2: do not edit by hand
% Please edit documentation in R/clean-address.R
\name{clean_addresses}
\alias{clean_addresses}
\title{Clean and standardise address fields}
\usage{
clean_addresses(data, id, address, city, zip, name = NULL, state = "NJ")
}
\arguments{
\item{data}{A data frame of records with addresses.}

\item{id}{Bare column name holding a unique record identifier.}

\item{address, city, zip}{Bare column names for the raw address parts.}

\item{name}{Optional bare column name for the record name (kept as
\code{record_name} for review/exports). Defaults to \code{NULL}.}

\item{state}{Two-letter state used for all rows. Defaults to \code{"NJ"} for the
first production workflow; pass another state abbreviation as needed.}
}
\value{
\code{data} with added columns: \code{record_id}, \code{record_name},
\code{address_raw}, \code{city_raw}, \code{zip_raw}, \code{address_clean}, \code{city_clean},
\code{state_clean}, \code{zip_clean}, \code{full_address_clean}.
}
\description{
Normalises raw address, city and ZIP text into geocoder-friendly columns and
builds a single-line \code{full_address_clean}. Column mappings are supplied with
tidy-eval (bare column names). The original columns are preserved; cleaned
values are written to new \verb{*_clean} columns, and a stable \code{record_id}
character key is created for downstream joins.
}
\examples{
df <- tibble::tibble(
  LocationID = "NJ306100", Name = "Hackensack-UMC Mountainside",
  Address = "ONE BAY AVE", City = "Montclair", Zip = "7042"
)
clean_addresses(df, id = LocationID, address = Address,
                       city = City, zip = Zip, name = Name)
}
