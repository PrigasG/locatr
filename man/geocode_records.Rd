% Generated by roxygen2: do not edit by hand
% Please edit documentation in R/geocode-cascade.R
\name{geocode_records}
\alias{geocode_records}
\title{Run the full geocoding cascade}
\usage{
geocode_records(
  data,
  tiers = c("census", "arcgis", "name"),
  reference = NULL,
  boundary = NULL,
  bbox = region_bbox("NJ"),
  verbose = TRUE
)
}
\arguments{
\item{data}{A data frame from \code{\link[=flag_bad_addresses]{flag_bad_addresses()}}.}

\item{tiers}{Which tiers to run, in order. Any subset of
\code{c("census", "arcgis", "name")}.}

\item{reference}{Optional trusted key -> coordinates table for Tier 0; see
\code{\link[=backfill_from_reference]{backfill_from_reference()}}. \code{NULL} skips Tier 0. For non-default column
names, call \code{\link[=backfill_from_reference]{backfill_from_reference()}} yourself before \code{\link[=geocode_records]{geocode_records()}}.}

\item{boundary}{Optional \code{sf} boundary for polygon-precise validation;
passed to \code{\link[=validate_geocodes]{validate_geocodes()}}. \code{NULL} uses the bounding box.}

\item{bbox}{Bounding box for region guards; see \code{\link[=region_bbox]{region_bbox()}}.}

\item{verbose}{Whether to print a per-tier match tally.}
}
\value{
\code{data} with coordinates and the full audit trail populated.
}
\description{
Orchestrates the tiered strategy on an already-cleaned, already-flagged frame
(see \code{\link[=clean_addresses]{clean_addresses()}} and \code{\link[=flag_bad_addresses]{flag_bad_addresses()}}): Census structured
match, then ArcGIS address fallback, then name lookup, validating against the
configured region after each tier so a later tier only retries what is still
unplaced.
}
\details{
Each tier records how it placed a row in \code{geocode_pass}, so the final frame is
self-documenting: \code{pass_0_reference}, \code{pass_1_census_structured},
\code{pass_2_fallback}, or \code{pass_4_name_lookup}. Anything still unmatched lands in
\code{needs_manual_review}.

Supplying \code{reference} runs an authoritative Tier 0 first
(\code{\link[=backfill_from_reference]{backfill_from_reference()}}): rows whose verified coordinates are already
known are placed exactly and skipped by every later tier. Feed prior cycles'
completed overrides back in here so manual review accrues over time.
}
