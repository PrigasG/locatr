% Generated by roxygen2: do not edit by hand
% Please edit documentation in R/backfill-from-reference.R
\name{backfill_from_reference}
\alias{backfill_from_reference}
\title{Backfill verified coordinates from a trusted reference table (Tier 0)}
\usage{
backfill_from_reference(
  data,
  reference,
  by = "record_id",
  lat_col = "latitude",
  lon_col = "longitude",
  county_col = NULL,
  locality_col = NULL,
  bbox = region_bbox("NJ")
)
}
\arguments{
\item{data}{A data frame from \code{\link[=flag_bad_addresses]{flag_bad_addresses()}} (or any frame carrying
the key column).}

\item{reference}{A data frame of verified records: the key column plus
coordinate columns. May also carry county/locality columns. \code{NULL} or an
empty frame makes this a no-op.}

\item{by}{Name of the key column shared by \code{data} and \code{reference}
(default \code{"record_id"}).}

\item{lat_col, lon_col}{Coordinate column names in \code{reference}
(default \code{"latitude"}/\code{"longitude"}).}

\item{county_col, locality_col}{Optional geography column names in \code{reference}
to backfill into \code{location_county}/\code{location_locality}.}

\item{bbox}{Bounding box used to reject out-of-region reference coordinates;
see \code{\link[=region_bbox]{region_bbox()}}.}
}
\value{
\code{data} with reference audit columns \code{ref_latitude}, \code{ref_longitude},
\code{ref_status}, and, for rows the reference filled, updated
\code{latitude}/\code{longitude}/\code{geocode_method}/\code{geocode_pass} (\code{"pass_0_reference"})/
\code{match_status}/\code{review_status}.
}
\description{
The authoritative first tier of the cascade. Joins coordinates from a curated
key -> coordinates table - an institutional-memory table of records whose
location was resolved and checked at some point - over the automated
geocoders, so a record that has already been verified never has to be
re-geocoded. This is what turns one analyst's manual review into a permanent
asset: feed last cycle's completed overrides (or any trusted coordinate list)
back in as \code{reference}, and those rows are placed instantly and exactly.
}
\details{
Reference coordinates are still bbox-validated, so a stale or fat-fingered
entry cannot drop a point outside the region. Because the reference is
authoritative, a matched row is marked \code{review_status == "reference_backfilled"}
and carries valid coordinates, so \code{\link[=geocode_census]{geocode_census()}} and every later tier skip
it automatically - even if its raw address was previously flagged (e.g. a
PO box whose true coordinates were verified once).

Run this before \code{\link[=geocode_census]{geocode_census()}} (the cascade does so when you pass
\verb{reference =} to \code{\link[=geocode_records]{geocode_records()}}).
}
\examples{
records <- tibble::tibble(
  record_id = c("a", "b"),
  review_status = c("ready_for_geocoding", "needs_manual_review")
)
verified <- tibble::tibble(record_id = "b", latitude = 40.22, longitude = -74.76)
backfill_from_reference(records, verified)
}
