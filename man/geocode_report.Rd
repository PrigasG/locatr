% Generated by roxygen2: do not edit by hand
% Please edit documentation in R/geocode-report.R
\name{geocode_report}
\alias{geocode_report}
\alias{print.locatr_report}
\title{Summarise a geocoding run into a provenance report}
\usage{
geocode_report(data, file = NULL, low_confidence_below = 0.5)

\method{print}{locatr_report}(x, ...)
}
\arguments{
\item{data}{A finished data frame from \code{\link[=geocode_records]{geocode_records()}} (ideally still
carrying its \code{locatr_run} manifest). At minimum, \code{review_status} and
\code{geocode_pass} drive the summary; \code{match_confidence} and \code{cache_status} are
used when present.}

\item{file}{Optional path. When given, a Markdown version of the report is
written there and the report object is returned invisibly.}

\item{low_confidence_below}{Confidence threshold (0-1) used to count
low-confidence rows in the summary. Defaults to \code{0.5}.}

\item{x}{A \code{locatr_report} object.}

\item{...}{Ignored.}
}
\value{
A \code{locatr_report} object (a named list) with the counts, confidence
summary, and \code{methods} paragraph. Printing it shows a formatted summary.
}
\description{
Turns a finished \code{\link[=geocode_records]{geocode_records()}} frame into an audit report: counts by
review status, by placing tier, and by cache status; a \code{match_confidence}
summary; and an auto-generated plain-language \emph{methods paragraph} suitable for
a report or a paper. When the run manifest is present (attached by
\code{\link[=geocode_records]{geocode_records()}}), the methods paragraph names the package versions, run
date, region guard, and cache activity; without it, the report is built from
the audit columns alone.
}
\examples{
df <- data.frame(
  record_id = c("a", "b", "c"),
  geocode_pass = c("pass_1_census_structured", "pass_2_fallback",
                   "pass_4_name_lookup"),
  review_status = c("auto_accepted", "auto_accepted", "needs_manual_review"),
  match_confidence = c(0.9, 0.72, 0.35)
)
geocode_report(df)
}
\seealso{
\code{\link[=geocode_records]{geocode_records()}}, \code{\link[=geocode_provenance]{geocode_provenance()}}, \code{\link[=add_match_confidence]{add_match_confidence()}}
}
